specVersion: "v1"
serviceId: "musegarden"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "musegarden-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
